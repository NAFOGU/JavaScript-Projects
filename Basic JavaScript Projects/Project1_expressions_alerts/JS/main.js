window.alert("Hello, World!");

var X = "Hello, World!";

document.write(X);

// The syntax below is JavaScript Function Syntax
// function name(parameter1, parameter2, parameter3) { Contains the code to be executed }

// The funtion below can be used to return the product of 2 numbers
function myFunction(a,b) {
    return a*b;
}

// The defined function above is called and the return value will be assigned to x
let x = myFunction(4, 3)